/**
 * The golden ratio (φ or phi), a mathematical constant approximately equal to 1.618.
 *
 * This constant appears in various mathematical contexts and has aesthetic properties.
 * In the delta-phi contrast algorithm, it's used as an exponent in the formula to
 * calculate the visual contrast between two colors in the Lab color space.
 *
 * Calculated as: (√5 + 1) / 2 or Math.pow(5, 0.5) * 0.5 + 0.5
 */
export const φ = 1.618033988749895;
